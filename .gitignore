# Local tooling / preview config
.claude/

# Internal setup notes (contain the shared-login email + default passwords — keep private)
SUPABASE-SETUP.md

# Supabase CLI local state (points at whichever project was last linked)
supabase/.temp/

# Screenshots may contain another centre's customer PII — ignore everything
# EXCEPT screenshots/manual/, which are generated Chadstone demo captures used
# by manual.html (fictional data, safe to publish).
screenshots/*
!screenshots/manual/
!screenshots/manual/**
